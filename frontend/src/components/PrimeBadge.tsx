import s from './PrimeBadge.module.css'

/** Prime-blue pill shown when a title is on Amazon Prime Video at no extra cost (FR). */
export function PrimeBadge() {
  return <span class={s.badge}>prime</span>
}
